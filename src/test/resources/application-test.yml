spring:
  datasource:
    url: jdbc:h2:mem:uniman
    username: sa
    password: password
    initialization-mode: always


  h2:
    console.enabled: true

  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: none
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  sql:
    init:
      mode: always
      data-locations: data-test.sql



key: Administratori123++/@
tokenValidity: 60*60